for disk in nvme0n1 nvme1n1; do sudo cryptsetup open --type plain -d /dev/urandom /dev/$disk to_be_wiped_$disk; done
for disk in nvme0n1 nvme1n1; do sudo cryptsetup open --type plain -d /dev/urandom /dev/$disk to_be_wiped_$disk; done
tmux ls | grep -vm1
tmux ls | grep -vm 1
tmux ls | grep -v -m1
tmux ls | grep -v -m 1
tmux ls | grep -m 1
tmux ls | grep -m 1 attached
echo $TMUX
cat bin/tmux-start 
tmux ls
tmux ls | grep -vm1 attached
tmux ls | grep -vm1 attached | cut -d: -f1
for disk in nvme0n1 nvme1n1; do sudo cryptsetup open --type plain -d /dev/urandom /dev/$disk to_be_wiped_$disk; done
gsettings
gsettings list schemas
gsettings list- schemas
gsettings list-schemas
gnome-schemas() { gsettings list-schemas; }
gnome-schemas
gnome_schemas=( gnome-schemas )
echo ${gnome_schemas}
gnome_schemas=( $(gnome-schemas) )
echo ${gnome_schemas[@]}
for schema in "${gnome_schemas[@]}"; do printf -- "\n$schema"; done
for schema in "${gnome_schemas[@]}"; do printf -- "\n$schema"; printf -- "\n"; done
for schema in "${gnome_schemas[@]}"; do printf -- "\n$schema"; done; printf -- "\n"
gnome-schemas | less
exiyt
emacs --execute='(dired "~/")'
emacs .
cd gits/bashrc_stuff/env
shellcheck .bashrc
ro install ShellCheck
journalctl -t 'rpm-ostree(unbound-libs.prein)
journalctl -t 'rpm-ostree(unbound-libs.prein)'
ro upgrade
sudo lsof /etc/group.4
sudo ls -Al /etc | grep group.4
sudo rm /etc/group.4
rpm-ostree upgrade
sudo killall tmux
pgrep tmux
for pid in $(command -p pgrep tmux); do pkill $pid; done
for pid in $(command -p pgrep tmux); do sudo kill -9 $pid; done
for pid in $(command -p pgrep tmux); do sudo kill -9 $pid; done
emacs .
debug emacs .
emacsctl status
systemctl --user --enable emacs
systemctl --user enable emacs
systemctl --user enable emacs | less
/usr/bin/emacs --help | less
/usr/bin/emacs --exec (calc)
/usr/bin/emacs --exec=(calc)
emacsclient --help | less
emacs < <(echo "(calc)")
emacs < $(echo "(calc)")
emacs <(echo "(calc)")
emacs -e < <(echo "(calc)")
emacs -e < <("(calc)")
emacs -e < <(printf -- "(calc)")
dired ./bin
emacsctl restart
emacs -e < <(printf -- "(calc (+ 3 4))")
emacs -e < <(printf -- "(calc + 3 4)")
sudo killall cat
sudo killall tmux
sudo killall gnome-terminal
pgrep tmux
for pid in command -p pgrep tmux|awk '{ print $1 }'; do kill -9 $pid; done
for pid in $(command -p pgrep tmux|awk '{ print $1 }'); do kill -9 $pid; done
lsblk
sudo su
emacs -e < <(printf -- "(calc)")
dired ./bin
emacsctl restart
emacs -e < <(printf -- "(calc (+ 3 4))")
emacs -e < <(printf -- "(calc + 3 4)")
sudo iotop
sudo iotop --pid 6926
sudo kill -9 6926
pgrep cat
sudo sync
sudo su
sudo tmux-start
sudo tmux
pgrep dd
sudo htop
top
top
hdparm --help | less 
hdparm -help | less 
man nvme-cli
man nvme
man nvme-format
virt-manager
cat /proc/meminfo | head -1 | awk '{print $2}'
echo $(( ( $(cat /proc/meminfo | head -1 | awk '{print $2}') / 1024 ) / 2 ))
rm crypto_*
rm -rf crypto_*
lsblk
findmnt
rm nohup.out 
rpm-ostree install --allow-inactive -A emacs-nox tmux libsodium libsodium-devel json-c json-c-devel libxml2 libxml2-devel ncurses ncurses-devel
rm nohup.out 
nano /etc/dnf/dnf.conf
sudo nano /etc/dnf/dnf.conf
dnf upgrade -y
sudo dnf upgrade -y
sudo dnf search libsodium
dnf search json-c
dnf search ncurses
dnf search libncurses
dnf search libxml2
dnf search ncurses
dnf search libncurses
dnf search libxml2
lslbk
umount /var/mnt
sudo umount /var/mnt
sudo umount /dev/sdc?
sudo eject /dev/sdc
umount /run/media/rescue/boot
sudo umount /run/media/rescue/boot
lsblk
lsblk | grep -v 'sdb|zram'
lsblk | grep -v 'sdb|zram0'
lsblk | grep -vE 'sdb|zram0'
la /
la /var
la /sysroot/
rpm-ostree status -v | less
la /var
la /sysroot/
rpm-ostree status -v | less
ro status
rm -rf .mozilla/
ll gits/user.js/
ll gits/user.js/extensions/
la .mozilla/
la .mozilla/firefox/
cat .mozilla/firefox/profiles.ini 
cd .mozilla
ll extensions/
ll extensions/\{ec8030f7-c20a-464f-9b0e-13a3a9e97384\}/
cd firefox/
rm -rf uu9zd0o3.default-release/
nano profiles.ini 
la azlgj4b7.ebeale.arkenfox/
rm -rf azlgj4b7.ebeale.arkenfox/
ll ct6kt7av.ebeale.arkenfox/
ll ~/gits/user.js/
cd ct6kt7av.ebeale.arkenfox/
cd ..
rm -rf ct6kt7av.ebeale.arkenfox/
ln -s /var/home/ebeale/gits/user.js $PWD/ct6kt7av.ebeale.arkenfox
github
gpg.elijahthomasbeale@gmail.com
gpg.elijahthomasbeale
cat .sshpw
cat .sshpw 
github.elijahthomasbeale
cat .bash_history 
dired
calc
cd gits/user.js/
pwd
man podman-run
podman run --rm=true dnf search json-c
podman run --rm=true run dnf search json-c
podman run --rm=true fedora run dnf search json-c
podman image list
podman rum --rm=true fedora dnf search json-c
podman run --rm=true fedora dnf search json-c
podman run --rm=true fedora -- echo "fastest_mirror=True" >> /etc/dnf/dnf.conf; echo "max_parallel_downloads=20 >> /etc/dnf/dnf.conf; dnf search json-c
podman run --rm=true fedora -- echo "fastest_mirror=True" >> /etc/dnf/dnf.conf; echo "max_parallel_downloads=20" >> /etc/dnf/dnf.conf; dnf search json-c
ls /etc/ | grep dnf
ls /etc/dnf/dnf.conf
sudo ls /etc/dnf/dnf.conf
sudo ls /etc/dnf/
sudo ls /etc/dnf/modules.d/
sudo ls /etc/dnf/*.d/
rpm-ostree install -A json-c-devel libsodium libsodium-devel ncurses-devel tmux emacs-nox parallel powertop
rpm-ostree install -A json-c-devel libsodium libsodium-devel ncurses-devel tmux emacs-nox parallel powertop
{ echo "fastest_mirror=True"; echo "max_parallel_downloads=20"; } >> /etc/dnf/dnf.conf
sudo { echo "fastest_mirror=True"; echo "max_parallel_downloads=20"; } >> /etc/dnf/dnf.conf
sudo su -c { echo "fastest_mirror=True"; echo "max_parallel_downloads=20"; } >> /etc/dnf/dnf.conf
sudo su -c "{ echo "fastest_mirror=True"; echo "max_parallel_downloads=20"; } >> /etc/dnf/dnf.conf"
cat /etc/dnf/dnf.conf
dnf upgrade -y && sudo dnf install -y gnome-shell-extension-gpaste gnome-tweaks
sudo su -c "dnf upgrade -y && dnf install -y gnome-shell-extension-gpaste gnome-tweaks"
gnome-tweaks
sudo dnf search gpaste
sudo remove gnome-shell-extension-gpaste
sudo dnf remove gnome-shell-extension-gpaste
podman ps
pgrep toolbox
pgrep toolbox
ip a
flatpak search ungoogled
flatpack upgrade
flatpak upgrade
flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
flaptpak search chromium
flatppak search chromium
flatpak search chromium
printf "%s\n" 'flatpack() { local input="${@:-$(</dev/stdin)}"; /usr/bin/flatpak "input"; }' >> .functions 
tail -3 .functions 
github.elijahthomasbeale 
pgrep toolbox
ip a
flatpak search ungoogled
flatpack upgrade
flatpak upgrade
flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
flaptpak search chromium
flatppak search chromium
flatpak search chromium
printf "%s\n" 'flatpack() { local input="${@:-$(</dev/stdin)}"; /usr/bin/flatpak "input"; }' >> .functions 
tail -3 .functions 
github.elijahthomasbeale 
flatpack search chromium
la gits/bashrc_stuff/env
ll gits/bashrc_stuff/env
chmod +x gits/bashrc_stuff/env/functions 
ll gits/bashrc_stuff/env/functions 
ll gits/bashrc_stuff/env/
chmod -x gits/bashrc_stuff/env/functions 
wich which
echo $BASH_SOURCE
echo ${BASH_SOURCE[@]}
declare -f
declare -f | less
declare -f | grep which
declare -f | grep flatpack
declare -f | grep -A 5 flatpack
declare -f | grep which
declare -f | grep flatpack
declare -f | grep -A 5 flatpack
unset -fn
unset -f
unset -f < <(cat .functions)
cat .functions
cat .functions | while read -r FUNCNAME; do echo $FUNCNAME; done
cat .functions | while read FUNCNAME; do echo $FUNCNAME; done
cat .functions | grep -E '^.*\(\)'
cat .functions | grep -E '^[a-z-]*\(\)'
cat .functions | grep -E '^[a-z-]*\(\)' | cut -d')' -f1
cat .functions | grep -E '^[a-z-]*\(\)' | cut -d'(' -f1
cat .functions | command -p grep -E '^[a-z-]*\(\)' | cut -d'(' -f1
printf "\e[m"
cat .{functions,translate} | command -p grep -E '^[a-z-]*\(\)' | cut -d'(' -f1
cat .{functions,translate} | command -p grep -E '^[^\s]*\(\)' | cut -d'(' -f1 | less
cat .{functions,translate} | command -p grep -E '^[a-z-]*\(\)' | cut -d'(' -f1 | less
printf "\n%s\n" 'cat .{functions,translate} | command -p grep -E '^[a-z-]*\(\)' | cut -d'(' -f1 | less' >> .functions
printf "\n%s\n" 'cat .{functions,translate} | command -p grep -E '^[a-z-]*\(\)' | cut -d\'(\' -f1' >> .functions
printf "\n%s\n" 'cat .{functions,translate} | command -p grep -E \'^[a-z-]*\(\)\' | cut -d\'(\' -f1' >> .functions
printf "\n%s\n" 'cat .{functions,translate} | command -p grep -E "^[a-z-]*\(\)" | cut -d"(" -f1' >> .functions
declare -f | grep -A 5 flatpack
while read -r FNAME; do echo "$FNAME"; done < <(cat .{functions,translate} | command -p grep -E "^[a-z-]*\(\)" | cut -d"\(" -f1)
while read -r FNAME; do echo "$FNAME"; done < <(cat .{functions,translate} | command -p grep -E "^[a-z-]*\(\)" | cut -d"(" -f1)
while read -r FNAME; do echo "$FNAME"; done < <(cat .{functions,translate} | command -p grep -E "^[a-z-]*\(\)" | cut -d"(" -f1) | less
rm .bash_profile && ln -s $PWD/.bash_profile $PWD/gits/bashrc_stuff/env/bashrc
rm .bash_profile && ln -s $PWD/gits/bashrc_stuff/env/bashrc $PWD/.bash_profile 
ln -s $PWD/gits/bashrc_stuff/env/bashrc $PWD/.bash_profile 
cat .bash_logout
cat .lesshst
echo > .lesshst 
rm .wget-hsts 
sudo killall tmux
pgrep tmux
for PID in $(command -p pgrep tmux); do pkill $PID; done
for PID in $(command -p pgrep tmux); do sudo kill -9 $PID; done
declare -f | grep -A 5 flatpack
github.elijahthomasbeale
cd .ssh
cat known_hosts
cat known_hosts.old 
rm known_hosts*
cat *.pub
cat *id*$
cat *id*
for key in *; do printf "%s\n" "$key"; cat "$key"; done
nano elijahthomasbeale+fedoraproject@gmail.com-id_ed25519.pub 
rm elijahthomasbeale+fedoraproject@gmail.com-id_ed25519*
ssh-add -L
ssh-add -l
man gnome-keyring
man seahorse
ip a
grep openwrt .spectre
grep openwrt .spectre
cd gits/bashrc_stuff/
git stash list
git-stash help
man git-stash
cat .aliases 
cat .aliases | command -p grep alias
cat .aliases | command -p grep alias | sed -r 's,alias\s,,g'
cat .aliases | command -p grep alias | sed -r -e 's,alias\s,,g' -e 's,=.*$,,g'
cat .aliases | sed -r -e 's,alias\s,,g' -e 's,=.*$,,g'
cat .aliases | sed -r -e 's,alias\s,,g' -e 's,=.*$,,g' -e ',^\#,d'
cat .aliases | sed -r -e 's,alias\s,,g' -e 's,=.*$,,g' -e '/^\#/d'
cat .aliases | sed -r -e 's,alias\s,,g' -e 's,=.*$,,g' -e '/^\#/d' -e '/^[]$/d'
cat .aliases | sed -r -e 's,alias\s,,g' -e 's,=.*$,,g' -e '/^\#/d' -e '/^\s*$/d'
cat .aliases | sed -r -e '/^\#/d' -e '/^\s*$/d' -e 's/\(alias\s\|=.*$\)//g'
cat .aliases | sed -r -e '/^\#/d' -e '/^\s*$/d' -e 's/\(^alias\s\|=.*$\)//g'
cat .aliases | sed -r -e '/^\#/d' -e '/^\s*$/d' -e 's/^alias\s//g'
cat .aliases | sed -r -e ':^\#:d' -e '/^\s*$/d' -e 's/^alias\s//g' -e 's/
cat .aliases | sed -r -e ':^\#:d' -e '/^\s*$/d' -e 's/^alias\s//g'
cat .aliases | sed -r -e ':^\#:d' -e ':^\s*$:d' -e 's:^alias\s::g' -e 's:=.*$::g'
cat .aliases | sed -r -e '/^\#/d' -e '/^\s*$/d' -e 's:^alias\s::g' -e 's:=.*$::g'
cat .aliases | sed -r -e '/^\#/d' -e '/^\s*$/d' -e 's/^alias\s//g' -e 's/=.*$//g'
while read -r ANAME; do echo "$ANAME"; done < <(cat $REPO/env/{aliases,spectre,sshpw} | sed -r -e '/^\#/d' -e '/^\s*$/d' -e 's/^alias\s//g' -e 's/=.*$//g')
while read -r ANAME; do echo "$ANAME"; done < <(cat $HOME/gits/bashrc_stuff/env/{aliases,spectre,sshpw} | sed -r -e '/^\#/d' -e '/^\s*$/d' -e 's/^alias\s//g' -e 's/=.*$//g')
while read -r ANAME; do echo "$ANAME"; done < <(cat $HOME/gits/bashrc_stuff/env/{aliases,spectre,sshpw} | sed -r -e '/^\#/d' -e '/^\s*$/d' -e 's/^alias\s//g' -e 's/=.*$//g') | less
while read -r ANAME; do echo "$ANAME"; done < <(sed -r -e '/^\#/d' -e '/^\s*$/d' -e 's/^alias\s//g' -e 's/=.*$//g' $HOME/gits/bashrc_stuff/env/{aliases,spectre,sshpw}) | less
grep -E ^alias
grep -E ^alias .aliases 
grep -E ^alias .aliases | command -p grep -vE 'alias|[\s='/]*'
grep -E ^alias .aliases | command -p grep -vE 'alias|[\s=\'/]*'
grep -E ^alias .aliases | command -p grep -vE "alias|[\s='/]*"
grep -E ^alias .aliases | command -p grep -vE "[\s='/]*"
wich ll
grep -E ^alias .aliases | command -p grep -vE "alias|[\s='/]*"
grep -E ^alias .aliases | command -p grep -vE "[\s='/]*"
wich ll
github.com
clip clear
github
clip empty
spectre -tx -a 1 github.com
grep -E ^alias .aliases | command -p grep -vE 'alias|[\s='/]*'
grep -E ^alias .aliases | command -p grep -vE 'alias|[\s=\'/]*'
grep -E ^alias .aliases | command -p grep -vE "alias|[\s='/]*"
grep -E ^alias .aliases | command -p grep -vE "[\s='/]*"
wich ll
github.com
clip clear
clip empty
spectre -tx -a 1 github.com
la .spectre.d/
crm.spectre.d/Elijah\ Thomas\ Beale.mpjson 
rm .spectre.d/Elijah\ Thomas\ Beale.mpjson 
sourc .spectre
github
cat .spectre.d/Elijah\ Thomas\ Beale.mpjson 
netflix
while read -r ANAME; do echo "$ANAME"; done < <(sed -r -e '/^\#/d' -e '/^\s*$/d' -e 's/^alias\s//g' -e 's/=.*$//g' $HOME/gits/bashrc_stuff/env/{aliases,spectre,sshpw}) | less
grep -E ^alias
grep -E ^alias .aliases 
grep -E ^alias .aliases | command -p grep -vE 'alias|[\s='/]*'
grep -E ^alias .aliases | command -p grep -vE 'alias|[\s=\'/]*'
grep -E ^alias .aliases | command -p grep -vE "alias|[\s='/]*"
grep -E ^alias .aliases | command -p grep -vE "[\s='/]*"
wich ll
github.com
clip clear
clip empty
spectre -tx -a 1 github.com
la .spectre.d/
crm.spectre.d/Elijah\ Thomas\ Beale.mpjson 
sourc .spectre
cat .spectre.d/Elijah\ Thomas\ Beale.mpjson 
netflix
rm .spectre.d/Elijah\ Thomas\ Beale.mpjson 
github
