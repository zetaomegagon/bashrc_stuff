for disk in nvme0n1 nvme1n1; do sudo cryptsetup open --type plain -d /dev/urandom /dev/$disk to_be_wiped_$disk; done
for disk in nvme0n1 nvme1n1; do sudo cryptsetup open --type plain -d /dev/urandom /dev/$disk to_be_wiped_$disk; done
tmux ls | grep -vm1
tmux ls | grep -vm 1
tmux ls | grep -v -m1
tmux ls | grep -v -m 1
tmux ls | grep -m 1
tmux ls | grep -m 1 attached
echo $TMUX
cat bin/tmux-start 
tmux ls
tmux ls | grep -vm1 attached
tmux ls | grep -vm1 attached | cut -d: -f1
for disk in nvme0n1 nvme1n1; do sudo cryptsetup open --type plain -d /dev/urandom /dev/$disk to_be_wiped_$disk; done
gsettings
gsettings list schemas
gsettings list- schemas
gsettings list-schemas
gnome-schemas() { gsettings list-schemas; }
gnome-schemas
gnome_schemas=( gnome-schemas )
echo ${gnome_schemas}
gnome_schemas=( $(gnome-schemas) )
echo ${gnome_schemas[@]}
for schema in "${gnome_schemas[@]}"; do printf -- "\n$schema"; done
for schema in "${gnome_schemas[@]}"; do printf -- "\n$schema"; printf -- "\n"; done
for schema in "${gnome_schemas[@]}"; do printf -- "\n$schema"; done; printf -- "\n"
gnome-schemas | less
exiyt
emacs --execute='(dired "~/")'
emacs .
cd gits/bashrc_stuff/env
shellcheck .bashrc
ro install ShellCheck
journalctl -t 'rpm-ostree(unbound-libs.prein)
journalctl -t 'rpm-ostree(unbound-libs.prein)'
ro upgrade
sudo lsof /etc/group.4
sudo ls -Al /etc | grep group.4
sudo rm /etc/group.4
rpm-ostree upgrade
sudo killall tmux
pgrep tmux
for pid in $(command -p pgrep tmux); do pkill $pid; done
for pid in $(command -p pgrep tmux); do sudo kill -9 $pid; done
for pid in $(command -p pgrep tmux); do sudo kill -9 $pid; done
emacs .
debug emacs .
emacsctl status
systemctl --user --enable emacs
systemctl --user enable emacs
systemctl --user enable emacs | less
/usr/bin/emacs --help | less
/usr/bin/emacs --exec (calc)
/usr/bin/emacs --exec=(calc)
emacsclient --help | less
emacs < <(echo "(calc)")
emacs < $(echo "(calc)")
emacs <(echo "(calc)")
emacs -e < <(echo "(calc)")
emacs -e < <("(calc)")
emacs -e < <(printf -- "(calc)")
dired ./bin
emacsctl restart
emacs -e < <(printf -- "(calc (+ 3 4))")
emacs -e < <(printf -- "(calc + 3 4)")
sudo killall cat
sudo killall tmux
sudo killall gnome-terminal
pgrep tmux
for pid in command -p pgrep tmux|awk '{ print $1 }'; do kill -9 $pid; done
for pid in $(command -p pgrep tmux|awk '{ print $1 }'); do kill -9 $pid; done
lsblk
sudo su
emacs -e < <(printf -- "(calc)")
dired ./bin
emacsctl restart
emacs -e < <(printf -- "(calc (+ 3 4))")
emacs -e < <(printf -- "(calc + 3 4)")
sudo iotop
sudo iotop --pid 6926
sudo kill -9 6926
pgrep cat
sudo sync
sudo su
sudo tmux-start
sudo tmux
pgrep dd
sudo htop
top
top
hdparm --help | less 
hdparm -help | less 
man nvme-cli
man nvme
man nvme-format
virt-manager
cat /proc/meminfo | head -1 | awk '{print $2}'
echo $(( ( $(cat /proc/meminfo | head -1 | awk '{print $2}') / 1024 ) / 2 ))
rm crypto_*
rm -rf crypto_*
lsblk
findmnt
rm nohup.out 
rpm-ostree install --allow-inactive -A emacs-nox tmux libsodium libsodium-devel json-c json-c-devel libxml2 libxml2-devel ncurses ncurses-devel
rm nohup.out 
nano /etc/dnf/dnf.conf
sudo nano /etc/dnf/dnf.conf
dnf upgrade -y
sudo dnf upgrade -y
sudo dnf search libsodium
dnf search json-c
dnf search ncurses
dnf search libncurses
dnf search libxml2
dnf search ncurses
dnf search libncurses
dnf search libxml2
lslbk
umount /var/mnt
sudo umount /var/mnt
sudo umount /dev/sdc?
sudo eject /dev/sdc
umount /run/media/rescue/boot
sudo umount /run/media/rescue/boot
lsblk
lsblk | grep -v 'sdb|zram'
lsblk | grep -v 'sdb|zram0'
lsblk | grep -vE 'sdb|zram0'
la /
la /var
la /sysroot/
rpm-ostree status -v | less
la /var
la /sysroot/
rpm-ostree status -v | less
ro status
rm -rf .mozilla/
ll gits/user.js/
ll gits/user.js/extensions/
la .mozilla/
la .mozilla/firefox/
cat .mozilla/firefox/profiles.ini 
cd .mozilla
ll extensions/
ll extensions/\{ec8030f7-c20a-464f-9b0e-13a3a9e97384\}/
cd firefox/
rm -rf uu9zd0o3.default-release/
nano profiles.ini 
la azlgj4b7.ebeale.arkenfox/
rm -rf azlgj4b7.ebeale.arkenfox/
ll ct6kt7av.ebeale.arkenfox/
ll ~/gits/user.js/
cd ct6kt7av.ebeale.arkenfox/
cd ..
rm -rf ct6kt7av.ebeale.arkenfox/
ln -s /var/home/ebeale/gits/user.js $PWD/ct6kt7av.ebeale.arkenfox
github
gpg.elijahthomasbeale@gmail.com
gpg.elijahthomasbeale
cat .sshpw
cat .sshpw 
github.elijahthomasbeale
cat .bash_history 
dired
calc
cd gits/user.js/
pwd
<<<<<<< Updated upstream
man podman-run
podman run --rm=true dnf search json-c
podman run --rm=true run dnf search json-c
podman run --rm=true fedora run dnf search json-c
podman image list
podman rum --rm=true fedora dnf search json-c
podman run --rm=true fedora dnf search json-c
podman run --rm=true fedora -- echo "fastest_mirror=True" >> /etc/dnf/dnf.conf; echo "max_parallel_downloads=20 >> /etc/dnf/dnf.conf; dnf search json-c
podman run --rm=true fedora -- echo "fastest_mirror=True" >> /etc/dnf/dnf.conf; echo "max_parallel_downloads=20" >> /etc/dnf/dnf.conf; dnf search json-c
ls /etc/ | grep dnf
ls /etc/dnf/dnf.conf
sudo ls /etc/dnf/dnf.conf
sudo ls /etc/dnf/
sudo ls /etc/dnf/modules.d/
sudo ls /etc/dnf/*.d/
rpm-ostree install -A json-c-devel libsodium libsodium-devel ncurses-devel tmux emacs-nox parallel powertop
rpm-ostree install -A json-c-devel libsodium libsodium-devel ncurses-devel tmux emacs-nox parallel powertop
{ echo "fastest_mirror=True"; echo "max_parallel_downloads=20"; } >> /etc/dnf/dnf.conf
sudo { echo "fastest_mirror=True"; echo "max_parallel_downloads=20"; } >> /etc/dnf/dnf.conf
sudo su -c { echo "fastest_mirror=True"; echo "max_parallel_downloads=20"; } >> /etc/dnf/dnf.conf
sudo su -c "{ echo "fastest_mirror=True"; echo "max_parallel_downloads=20"; } >> /etc/dnf/dnf.conf"
cat /etc/dnf/dnf.conf
dnf upgrade -y && sudo dnf install -y gnome-shell-extension-gpaste gnome-tweaks
sudo su -c "dnf upgrade -y && dnf install -y gnome-shell-extension-gpaste gnome-tweaks"
gnome-tweaks
sudo dnf search gpaste
sudo remove gnome-shell-extension-gpaste
sudo dnf remove gnome-shell-extension-gpaste
podman ps
pgrep toolbox
=======
rpm-ostree upgrade && rpm-ostree install -A tmux emacs-nox json-c-devel libsodium libsodium-devel ncurses-devel gpaste
rpm-ostree status
reboot
>>>>>>> Stashed changes
